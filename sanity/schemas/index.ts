// All schemas registered here — add new schema types to this array.
import { siteSettings } from './siteSettings'
import { testimonial }  from './testimonial'
import { property }     from './property'

export const schemaTypes = [siteSettings, testimonial, property]
